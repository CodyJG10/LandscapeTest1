# Site identity
SITE_NAME={{ site_name }}
COMPOSE_PROJECT_NAME={{ site_name | lower }}

# Host port mappings (must be unique per site on a shared VPS)
STRAPI_PORT={{ strapi_port | default('1337') }}
ADMINER_PORT={{ adminer_port | default('9090') }}

# Strapi application
HOST={{ strapi_host | default('0.0.0.0') }}
PORT=1337
APP_KEYS="{{ strapi_app_keys }}"
API_TOKEN_SALT={{ strapi_api_token_salt }}
ADMIN_JWT_SECRET={{ strapi_admin_jwt_secret }}
TRANSFER_TOKEN_SALT={{ strapi_transfer_token_salt }}
JWT_SECRET={{ strapi_jwt_secret }}
ENCRYPTION_KEY={{ strapi_encryption_key }}

# Database — DATABASE_HOST must match the Compose service name
DATABASE_CLIENT={{ db_client | default('postgres') }}
DATABASE_HOST=strapiDB
DATABASE_NAME={{ db_name }}
DATABASE_USERNAME={{ db_username }}
DATABASE_PASSWORD={{ db_password }}
DATABASE_PORT={{ db_port | default('5432') }}

NODE_ENV={{ node_env | default('production') }}

# Public URL — tells Strapi its own address (used for redirects and allowedHosts)
PUBLIC_URL=https://{{ domain_name }}
ALLOWED_HOSTS={{ domain_name }}
